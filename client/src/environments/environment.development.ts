export const environment = {

    production: false, //development environment, thus initialize as false. 
    apiUrl: 'https://localhost:5001/api/' //endpoint of the server in the local host, as is dev environment 
};


//GOTO: Environemnt.ts for more notes 